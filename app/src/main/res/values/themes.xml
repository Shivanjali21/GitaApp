<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShreeBhagavadGita" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="android:statusBarColor">@color/colorBg</item>
         <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.ShreeBhagavadGita" parent="Base.Theme.ShreeBhagavadGita"/>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.MinWidth">
        <!-- Used for the buttons -->
        <item name="colorAccent">@color/colorOrange</item>

        <!-- Used for the title and text -->
        <item name="android:textColorPrimary">@color/colorBlack</item>
        <!-- Used for the background -->
        <item name="android:background">@color/colorWhite</item>
    </style>
</resources>